<Window x:Class="NorthwindApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Pedidos - Northwind" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título principal -->
        <TextBlock Grid.Row="0" Text="Consulta de Pedidos (Northwind DB)" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2c3e50"/>

        <!-- Panel de búsqueda -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Label Content="Buscar Cliente:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtFiltro" Width="250" Height="28" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
            <Button x:Name="btnBuscar" Content="Buscar" Width="90" Height="28" Click="BtnBuscar_Click" Margin="0,0,5,0"/>
            <Button x:Name="btnMostrarTodo" Content="Mostrar Todo" Width="100" Height="28" Click="BtnMostrarTodo_Click"/>
        </StackPanel>

        <!-- DataGrid configurado -->
        <DataGrid x:Name="dgPedidos" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" GridLinesVisibility="Horizontal" 
                  RowBackground="#FAFAFA" AlternatingRowBackground="#FFFFFF">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding OrderID}" Width="80"/>
                <DataGridTextColumn Header="Nombre del Cliente" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="Fecha de Pedido" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy}" Width="150"/>
                <DataGridTextColumn Header="Monto del Flete ($)" Binding="{Binding Freight, StringFormat=C2}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Barra de estado -->
        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem x:Name="sbiCantidad" Content="Pedidos: 0" Margin="0,0,15,0"/>
            <Separator Width="1" Height="15"/>
            <StatusBarItem x:Name="sbiTotalFlete" Content="Total Flete: $0.00" Margin="15,0,15,0"/>
            <Separator Width="1" Height="15"/>
            <StatusBarItem x:Name="sbiPromedioFlete" Content="Promedio Flete: $0.00" Margin="15,0,0,0"/>
        </StatusBar>
    </Grid>
</Window>