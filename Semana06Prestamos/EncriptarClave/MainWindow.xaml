<Window x:Class="EncriptarClave.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EncriptarClave"
        mc:Ignorable="d"
        Title="Encriptar y Desencriptar" Height="420" Width="500" MinWidth="400" MinHeight="350">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" 
               Content="Clave:" 
               VerticalAlignment="Center" HorizontalAlignment="Right"
               FontSize="14" FontWeight="Bold" Margin="5"/>

        <TextBox Grid.Row="0" Grid.Column="1" 
                 x:Name="tbxClave" 
                 VerticalAlignment="Center" 
                 FontSize="14" Margin="5" Padding="3"/>

        <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                x:Name="btnEncriptar" 
                Content="Encriptar" 
                HorizontalAlignment="Center" 
                Padding="10,5" Margin="5" Click="btnEncriptar_Click"/>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                    VerticalAlignment="Center" Margin="0,10">

            <Label Content="Clave encriptada:" 
                   HorizontalAlignment="Center" 
                   FontSize="14" FontWeight="Bold"/>
            <TextBox x:Name="tbxResultado" 
                     IsReadOnly="True"
                     HorizontalAlignment="Center" 
                     Width="250" FontSize="16" Margin="5" Padding="3" IsReadOnlyCaretVisible="True"/>

            <Button x:Name="btnDesencriptar" 
                    Content="Desencriptar clave" 
                    HorizontalAlignment="Center" 
                    Padding="10,5" Margin="15,10,15,5" Click="btnDesencriptar_Click"/>

            <Label Content="Clave desencriptada:" 
                   HorizontalAlignment="Center" 
                   FontSize="14" FontWeight="Bold"/>
            <TextBox x:Name="tbxDesencriptado" 
                     IsReadOnly="True"
                     HorizontalAlignment="Center" 
                     Width="250" FontSize="16" Margin="5" Padding="3" IsReadOnlyCaretVisible="True"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="Sistema de seguridad activo" 
                   Foreground="Gray" FontSize="10" HorizontalAlignment="Center" Margin="5"/>

    </Grid>
</Window>